import Image from 'next/image';

const PaymentMethods = () => {
    return (
        <Image
            src={'/example.webp'}
            alt={'data'}
            layout="fill"
            objectFit="contain"
        />
    );
};

export default PaymentMethods;
